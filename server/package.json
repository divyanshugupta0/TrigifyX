{
  "name": "trigifyx-server",
  "version": "1.0.0",
  "description": "TrigifyX backend: authenticates capture requests via access tokens, resolves Telegram destinations from Firebase, and delivers form submissions to Telegram. Keeps the bot token 100% server-side.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.1.1"
  }
}
